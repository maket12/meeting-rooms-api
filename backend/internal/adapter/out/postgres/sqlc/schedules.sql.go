// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: schedules.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createSchedule = `-- name: CreateSchedule :one
INSERT INTO schedules (
    id,
    room_id,
    days_of_week,
    start_minutes,
    end_minutes
) VALUES (
    $1,
    $2,
    $3,
    $4,
    $5
)
RETURNING id, room_id, days_of_week, start_minutes, end_minutes
`

type CreateScheduleParams struct {
	ID           pgtype.UUID
	RoomID       pgtype.UUID
	DaysOfWeek   []int32
	StartMinutes int32
	EndMinutes   int32
}

func (q *Queries) CreateSchedule(ctx context.Context, db DBTX, arg CreateScheduleParams) (Schedule, error) {
	row := db.QueryRow(ctx, createSchedule,
		arg.ID,
		arg.RoomID,
		arg.DaysOfWeek,
		arg.StartMinutes,
		arg.EndMinutes,
	)
	var i Schedule
	err := row.Scan(
		&i.ID,
		&i.RoomID,
		&i.DaysOfWeek,
		&i.StartMinutes,
		&i.EndMinutes,
	)
	return i, err
}

const getScheduleByRoomID = `-- name: GetScheduleByRoomID :one
SELECT
    id,
    room_id,
    days_of_week,
    start_minutes,
    end_minutes
FROM schedules
WHERE room_id = $1
`

func (q *Queries) GetScheduleByRoomID(ctx context.Context, db DBTX, roomID pgtype.UUID) (Schedule, error) {
	row := db.QueryRow(ctx, getScheduleByRoomID, roomID)
	var i Schedule
	err := row.Scan(
		&i.ID,
		&i.RoomID,
		&i.DaysOfWeek,
		&i.StartMinutes,
		&i.EndMinutes,
	)
	return i, err
}
