// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: rooms.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createRoom = `-- name: CreateRoom :one
INSERT INTO rooms (
    id,
    name,
    description,
    capacity,
    created_at
) VALUES (
    $1, $2, $3, $4, $5
)
RETURNING id, name, description, capacity, created_at
`

type CreateRoomParams struct {
	ID          pgtype.UUID
	Name        string
	Description pgtype.Text
	Capacity    int32
	CreatedAt   pgtype.Timestamptz
}

func (q *Queries) CreateRoom(ctx context.Context, db DBTX, arg CreateRoomParams) (Room, error) {
	row := db.QueryRow(ctx, createRoom,
		arg.ID,
		arg.Name,
		arg.Description,
		arg.Capacity,
		arg.CreatedAt,
	)
	var i Room
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Description,
		&i.Capacity,
		&i.CreatedAt,
	)
	return i, err
}

const getRoomByID = `-- name: GetRoomByID :one
SELECT
    id,
    name,
    description,
    capacity,
    created_at
FROM rooms
WHERE id = $1
`

func (q *Queries) GetRoomByID(ctx context.Context, db DBTX, id pgtype.UUID) (Room, error) {
	row := db.QueryRow(ctx, getRoomByID, id)
	var i Room
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Description,
		&i.Capacity,
		&i.CreatedAt,
	)
	return i, err
}

const listRooms = `-- name: ListRooms :many
SELECT
    id,
    name,
    description,
    capacity,
    created_at
FROM rooms
`

func (q *Queries) ListRooms(ctx context.Context, db DBTX) ([]Room, error) {
	rows, err := db.Query(ctx, listRooms)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Room
	for rows.Next() {
		var i Room
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Description,
			&i.Capacity,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
